import styled from "styled-components";

export const PersonFilterWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 5px;
  
  button {
    padding: 0;
    border: none;
    background-color: inherit;
  }
`